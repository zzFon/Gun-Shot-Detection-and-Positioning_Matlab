

%[x,fs] = audioread('s1.wav');

%%% ∑÷÷° %%%
N = 800; % ÷°≥§/¥∞≥§
inc = round(N/3); % ÷°“∆
wnd = hamming(N); % (4-3)hamming¥∞
frames = enframe(x',wnd,inc); % ∑÷÷°
fprintf('frames: \n');
size(frames)

%%% draw x %%%
p2 = abs(fft(x/length(gun0)));
mixed_fft = p2(1:length(x)/2+1);
mixed_fft(2:end-1) = 2*mixed_fft(2:end-1);
subplot(2,1,1);plot(x);subplot(2,1,2);plot(mixed_fft);

[dim,data_sz] = size(x');


